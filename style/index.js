import colors from './colors';
import utils from './utils';
import fonts from './fonts';
import components from './components';

const theme = { colors, fonts, utils, components };

export default theme;
